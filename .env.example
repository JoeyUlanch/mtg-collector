# Copy to .env and customize (optional — docker compose reads this)
PORT=3847
SESSION_SECRET=replace-with-a-long-random-string

# Optional: full URL shown in Settings for phone access
# PUBLIC_URL=http://192.168.1.20:3847
